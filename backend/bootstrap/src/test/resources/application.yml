spring:
  profiles:
    active: test
  datasource:
    url: jdbc:postgresql://localhost:5433/hisaberp_test
    username: hisaberp_app
    password: hisaberp
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    contexts: default
    url: jdbc:postgresql://localhost:5433/hisaberp_test
    user: hisaberp
    password: hisaberp
  mail:
    host: localhost
    port: 25
  data:
    redis:
      host: localhost
      port: 6380
  cache:
    type: simple
  modulith:
    events:
      jdbc-schema-initialization:
        enabled: true

app:
  security:
    jwt:
      secret: TEST_SECRET_AT_LEAST_32_CHARACTERS_LONG_FOR_HS256_OK
  storage:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: hisaberp-test
    max-file-size-bytes: 5242880
