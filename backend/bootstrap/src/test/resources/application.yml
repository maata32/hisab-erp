spring:
  profiles:
    active: test
  datasource:
    url: jdbc:postgresql://localhost:5433/minierp_test
    username: minierp_app
    password: minierp
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    contexts: default
    url: jdbc:postgresql://localhost:5433/minierp_test
    user: minierp
    password: minierp
  mail:
    host: localhost
    port: 25
  data:
    redis:
      host: localhost
      port: 6380
  cache:
    type: simple
  modulith:
    events:
      jdbc-schema-initialization:
        enabled: true

app:
  security:
    jwt:
      secret: TEST_SECRET_AT_LEAST_32_CHARACTERS_LONG_FOR_HS256_OK
