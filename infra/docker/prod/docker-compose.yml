# Mini-ERP — production stack
# Designed for a single Hetzner CX31/CX41 node (8GB+) with Docker Compose.
# Scale-out: split DB to Hetzner-managed PG, run multiple backend replicas behind Traefik.
#
# Required env vars (set in /etc/minierp/.env on the host, root-only readable):
#   ROOT_DOMAIN              minierp.example.com
#   ACME_EMAIL               ops@example.com           (Let's Encrypt)
#   POSTGRES_PASSWORD        strong random  (owner/admin role 'minierp' — Liquibase only)
#   APP_DB_PASSWORD          strong random  (runtime role 'minierp_app' — Spring datasource)
#   REDIS_PASSWORD           strong random
#   JWT_SECRET               >= 32 chars random
#   PII_ENCRYPTION_KEY       32 chars
#   MINIO_ROOT_PASSWORD      strong random
#   GRAFANA_ADMIN_PASSWORD   strong random
#   APP_VERSION              git tag
#   IMAGE_REGISTRY           ghcr.io/your-org           (or registry.hetzner.com/...)

name: minierp-prod

services:
  traefik:
    image: traefik:v3.1
    container_name: traefik
    restart: unless-stopped
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entryPoints.web.address=:80"
      - "--entryPoints.websecure.address=:443"
      - "--entryPoints.web.http.redirections.entryPoint.to=websecure"
      - "--entryPoints.web.http.redirections.entryPoint.scheme=https"
      - "--certificatesResolvers.le.acme.email=${ACME_EMAIL}"
      - "--certificatesResolvers.le.acme.storage=/letsencrypt/acme.json"
      - "--certificatesResolvers.le.acme.tlsChallenge=true"
      - "--accesslog=true"
      - "--accesslog.filepath=/var/log/traefik/access.log"
      - "--metrics.prometheus=true"
      - "--metrics.prometheus.entryPoint=metrics"
      - "--entryPoints.metrics.address=:8082"
      - "--api.dashboard=false"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik-acme:/letsencrypt
      - traefik-logs:/var/log/traefik
    networks: [minierp-public, minierp-internal]
    deploy:
      resources:
        limits: { memory: 256M }

  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: minierp
      POSTGRES_USER: minierp
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --locale=C"
      # Consumed by postgres-init.sh to set the runtime role's password on first boot.
      APP_DB_PASSWORD: ${APP_DB_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ../../../scripts/postgres-init.sh:/docker-entrypoint-initdb.d/00-init.sh:ro
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "minierp"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [minierp-internal]
    deploy:
      resources:
        limits: { memory: 2G }

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}", "--save", "60", "1", "--loglevel", "warning", "--maxmemory", "512mb", "--maxmemory-policy", "allkeys-lru"]
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [minierp-internal]
    deploy:
      resources:
        limits: { memory: 768M }

  minio:
    image: minio/minio:RELEASE.2024-09-13T20-26-02Z
    container_name: minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:9000/minio/health/ready"]
      interval: 30s
      timeout: 5s
      retries: 6
    networks: [minierp-internal]
    deploy:
      resources:
        limits: { memory: 768M }

  backend:
    image: ${IMAGE_REGISTRY}/minierp-backend:${APP_VERSION}
    container_name: backend
    restart: unless-stopped
    depends_on:
      postgres: { condition: service_healthy }
      redis:    { condition: service_healthy }
      minio:    { condition: service_healthy }
    environment:
      SPRING_PROFILES_ACTIVE: prod
      APP_ENV: prod
      APP_VERSION: ${APP_VERSION}
      APP_PUBLIC_URL: https://api.${ROOT_DOMAIN}
      APP_CORS_ALLOWED_ORIGINS: https://app.${ROOT_DOMAIN},https://pos.${ROOT_DOMAIN}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/minierp
      # Runtime pool connects as the NON-SUPERUSER role so PostgreSQL RLS (defense layer 2)
      # is enforced at runtime. A superuser/BYPASSRLS runtime role silently defeats RLS.
      SPRING_DATASOURCE_USERNAME: minierp_app
      SPRING_DATASOURCE_PASSWORD: ${APP_DB_PASSWORD}
      # Liquibase runs DDL as the owner/admin role (separate from the runtime pool).
      SPRING_LIQUIBASE_URL: jdbc:postgresql://postgres:5432/minierp
      SPRING_LIQUIBASE_USER: minierp
      SPRING_LIQUIBASE_PASSWORD: ${POSTGRES_PASSWORD}
      DB_POOL_MAX: "30"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS_TTL: PT15M
      JWT_REFRESH_TTL: P7D
      PII_ENCRYPTION_KEY: ${PII_ENCRYPTION_KEY}
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: minio
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      S3_BUCKET: minierp-documents
      MAIL_HOST: ${MAIL_HOST:-}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_USERNAME: ${MAIL_USERNAME:-}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-}
      MAIL_FROM: noreply@${ROOT_DOMAIN}
      JAVA_OPTS: "-XX:+UseG1GC -XX:MaxRAMPercentage=75 -XX:+ExitOnOutOfMemoryError"
      LOG_LEVEL_ROOT: WARN
      LOG_LEVEL_APP: INFO
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/actuator/health/liveness"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 90s
    networks: [minierp-internal, minierp-public]
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=minierp_minierp-public"
      - "traefik.http.routers.api.rule=Host(`api.${ROOT_DOMAIN}`)"
      - "traefik.http.routers.api.entrypoints=websecure"
      - "traefik.http.routers.api.tls.certresolver=le"
      - "traefik.http.services.api.loadbalancer.server.port=8080"
      - "traefik.http.middlewares.api-ratelimit.ratelimit.average=100"
      - "traefik.http.middlewares.api-ratelimit.ratelimit.burst=200"
      - "traefik.http.routers.api.middlewares=api-ratelimit"
    deploy:
      resources:
        limits: { memory: 1.5G }

  admin:
    image: ${IMAGE_REGISTRY}/minierp-admin:${APP_VERSION}
    container_name: admin
    restart: unless-stopped
    depends_on: [backend]
    networks: [minierp-internal, minierp-public]
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=minierp_minierp-public"
      - "traefik.http.routers.admin.rule=Host(`app.${ROOT_DOMAIN}`)"
      - "traefik.http.routers.admin.entrypoints=websecure"
      - "traefik.http.routers.admin.tls.certresolver=le"
      - "traefik.http.services.admin.loadbalancer.server.port=80"
    deploy:
      resources:
        limits: { memory: 128M }

  pos:
    image: ${IMAGE_REGISTRY}/minierp-pos:${APP_VERSION}
    container_name: pos
    restart: unless-stopped
    depends_on: [backend]
    networks: [minierp-internal, minierp-public]
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=minierp_minierp-public"
      - "traefik.http.routers.pos.rule=Host(`pos.${ROOT_DOMAIN}`)"
      - "traefik.http.routers.pos.entrypoints=websecure"
      - "traefik.http.routers.pos.tls.certresolver=le"
      - "traefik.http.services.pos.loadbalancer.server.port=80"
    deploy:
      resources:
        limits: { memory: 128M }

  pgbackup:
    image: prodrigestivill/postgres-backup-local:16-alpine
    container_name: pgbackup
    restart: unless-stopped
    depends_on:
      postgres: { condition: service_healthy }
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: minierp
      POSTGRES_USER: minierp
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_EXTRA_OPTS: "-Z9 --schema=public --blobs"
      SCHEDULE: "@daily"
      BACKUP_KEEP_DAYS: "14"
      BACKUP_KEEP_WEEKS: "8"
      BACKUP_KEEP_MONTHS: "12"
      HEALTHCHECK_PORT: "8081"
    volumes:
      - pgbackup-data:/backups
    networks: [minierp-internal]
    deploy:
      resources:
        limits: { memory: 256M }

networks:
  minierp-public:
    name: minierp_minierp-public
  minierp-internal:
    internal: true

volumes:
  postgres-data:
  redis-data:
  minio-data:
  traefik-acme:
  traefik-logs:
  pgbackup-data:
