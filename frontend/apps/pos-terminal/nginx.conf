server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # PWA service worker MUST never be cached aggressively
  location = /ngsw-worker.js          { add_header Cache-Control "no-cache, must-revalidate" always; }
  location = /ngsw.json               { add_header Cache-Control "no-cache, must-revalidate" always; }
  location = /manifest.webmanifest    { add_header Cache-Control "no-cache, must-revalidate" always; }

  location ~* \.(?:css|js|woff2?|ttf|eot|png|jpg|jpeg|gif|webp|svg|ico)$ {
    expires 1y;
    access_log off;
    add_header Cache-Control "public, immutable";
  }

  location /api/      { proxy_pass http://backend:8080; include /etc/nginx/conf.d/proxy.conf; }
  location /actuator/ { proxy_pass http://backend:8080; include /etc/nginx/conf.d/proxy.conf; }

  location / {
    try_files $uri $uri/ /index.html;
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  }

  gzip on;
  gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/wasm;
  gzip_min_length 1024;
}
